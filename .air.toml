# Air configuration for hot-reload development
root = "."
testdata_dir = "testdata"
tmp_dir = "tmp"

[build]
  args_bin = ["dev", "examples/rest-api/main.glyph", "--port", "8080"]
  bin = "./tmp/glyph"
  cmd = "go build -o ./tmp/glyph ./cmd/glyph"
  delay = 1000
  exclude_dir = ["assets", "tmp", "vendor", "testdata", "target", "docs", "examples"]
  exclude_file = []
  exclude_regex = ["_test.go", "_bench_test.go"]
  exclude_unchanged = false
  follow_symlink = false
  full_bin = ""
  include_dir = []
  include_ext = ["go", "glyph"]
  include_file = []
  kill_delay = "0s"
  log = "build-errors.log"
  poll = false
  poll_interval = 0
  rerun = false
  rerun_delay = 500
  send_interrupt = false
  stop_on_error = false

[color]
  app = ""
  build = "yellow"
  main = "magenta"
  runner = "green"
  watcher = "cyan"

[log]
  main_only = false
  time = false

[misc]
  clean_on_exit = false

[screen]
  clear_on_rebuild = false
  keep_scroll = true
