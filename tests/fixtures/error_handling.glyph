# Route with error handling
: Error {
  code: str!
  message: str!
}

: DataResponse {
  data: str!
}

@ GET /api/data/:id -> DataResponse {
  % db: Database
  $ result = db.data.get(id)
  > result
}
