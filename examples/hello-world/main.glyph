# Hello World Example
# Basic GlyphLang route demonstrating simple response

@ GET / {
  > {
    text: "Hello, World!",
    timestamp: 1234567890
  }
}

# Greeting with path parameter
@ GET /hello/:name {
  $ greeting = "Hello, " + name + "!"
  > {message: greeting}
}

# Health check endpoint
@ GET /health {
  > {status: "ok"}
}
